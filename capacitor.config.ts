import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.titaniumbones.soshreds',
  appName: 'SO Shreds',
  webDir: 'dist',
}

export default config
